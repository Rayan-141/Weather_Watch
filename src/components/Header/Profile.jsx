import React from 'react';
import './Header.css'; // Reusing Header styles

const Profile = () => {
    return (
        <i className="bi bi-person-circle profile-icon"></i>
    );
};

export default Profile;
